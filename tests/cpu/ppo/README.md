# PPO Algorithm Invariants

These tests validate the correctness of PPO components:

- Policy loss
- Value loss
- Entropy bonus
- KL divergence behavior
- KL watchdog triggering
- Value monotonicity

These invariants ensure PPO behaves as expected under controlled conditions.